server:
  port: 9090
  error:
    include-stacktrace: never
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/db_mars
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
